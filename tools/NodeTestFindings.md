# 规则图节点实测 · 问题挂账（findings ledger）

## 📌 最终状态（2026-09-19 收尾定版）
> **全量 301 节点 + 回归族 = 309 用例 → 309/309 全部通过** ✓（含 F13/F14 修复后复验）
> 结果存档：`tools/report_all.txt`；用例表：`tools/all_cases.tsv`（自动生成 + `tools/curated.tsv` 人工覆盖）

| 状态 | 条目 |
|---|---|
| ✅ **已修复+验证** | **F1** 英雄濒死 · **F2** 单值卡通道(101007) · **F4** 牌堆族(105002/105003/105004) · **F5** 映射 Object 通道 · **F6** 103003 栈溢出 · **F7** 103010-103012 定义取值 · **F8** 快照族 · **F9** 事件记录口 · **F10** 108011 事件回合 · **F11** 增益族（端到端）· **F12** 112007 Object 通道 |
| ✅ **查明非缺陷（此前误报）** | **F3** 102029（英雄无牌堆）· **F11 增益"取不到"**（= 探针缺 BuffDefine 通道，产品侧 106002/206001 两条路径实测正常）· **105002**（返回的就是来源牌堆的牌；此前空集合是"来源指向玩家1 的空牌库"） |
| 🔧 **本次新增的区域名统一** | 新增 `ZoneNames.cs`（显示名/内部名/别名唯一口径），`PileNormalize`/`PileDecode`/`PileList`/`ZoneCards`/`PileMoveCard` 与编辑器 4 处下拉全部改为引用它 —— 装备区→装备、奥秘区→奥秘 实测一致 ✓ |
| ✅ **本轮再修（F13/F14，309/309 通过）** | **F13** `102018 是否濒死`：英雄识别原用"与当前英雄实例同一对象"（`owner.hero.uid == card.uid`）→ 来源卡换了实例就漏判、退回 `GetHP()<=0` → 布尔口/条件口**恒真**。改用引擎自己的判据 **`CardData.type == CardType.Hero`**（同 `GameLogic.IsHeroCard`），英雄一律读所属玩家 HP → 存活态 false / 濒死态 true **成对验证** ✓ · **F14** `106006 取不到卡上增益` = **非缺陷**：① 探针缺 `Buff` 通道（`ResolveBuffList`）；② 长会话里前面的「沉默(202015)/消灭(202016)」用例把卡上增益清掉（**顺序依赖**）→ 用例改为"自带施加前提"（并在池被清空时重载）→ 实测 `caster.buffs=1`、`ResolveBuffList 结果=1` ✓；顺带给产品 `GetObjectInput` 补了 `106006` 分支（Object 口也能取到）|

### 🔧 本次修掉的 4 处"测试台自身"问题（重要，直接影响结论可信度）
1. **动作/取值分类判据**：原来全文搜 `case "id":` → 把取值器里的 case 也算动作 ✗（实测 `102018` 被当动作 → "未支持动作"→ 缺省断言**假通过**）。改为只在 `ExecuteAction` 方法体内搜（L2142~L3904）✓
2. **`exec` 断言加护栏**：日志出现「未支持的 NodeDoc 动作」即判失败（防止"动作数≥1 其实只是入口推进"的假通过）✓
3. **类型匹配改全等**（仅 `NodeValueRef<...>` 包装类型按包含）：原来 `"CardSnapshot".Contains("Card")` 把**卡牌**来源接到了**快照**口 ✗（104006~104009 全空）✓
4. **补 3 类取值通道/兜底**：`BuffDefine`（ResolveValueBuffDefine）、`Pile`（ResolveValuePile）、非卡牌类型集合的 `ConvertTo{Defines,Buffs,Events}` 兜底 ✓

### ⚠️ 本次自查到的**我自己的错误**（重要，避免再犯）
1. **探针 `AddExtraSource` 没把新增来源种类接上** ✗（只加在 `CreateSource` 里）→ 导致 **SNAPSHOT/PILE/BUFFDEF 三类用例的来源线从未真正连上**，于是 F8/F4/F11 的"失败"**大部分是探针的假问题** ✗。修法：统一走 `CreateSource` + 加诊断日志 `[探针接线] … 已接=True/False` 自证 ✓。**结论：报"某族全挂"之前，先证明来源线真的接上了。**
2. `value_notnull` 曾把**空串**当作"有值" → 造成 **F4/F8/F11 的假通过** ✗。已修严（空串不算有值 ✓），并因此暴露出 F8/F4/F11 三个真问题。
3. 编译判据不可信 → 已写入 skill `.codebuddy/skills/unity-compile-before-play/SKILL.md` ✓（`clear_unity_logs` → `refresh_editor` → `compile_scripts` → 等 12s → grep `error CS` 必须 0 条 ✓）；本轮靠它当场抓到 `CS7036`、`CS0152`、`CS0103` 三个错误 ✓✓。


> 约定：批次测试期间**只记录不修改**，全部批次跑完后再统一修复 + 回归（避免打断测试节奏）。
> 每条包含：现象 / 证据 / 影响 / 建议修法 / 状态。

## F1. 102018「是否濒死」对**英雄卡**恒为真
- **现象**：`card←英雄卡` 时返回 `True`（甚至满血）。
- **证据**：实现为 `c.GetHP() <= 0`（`NodeDocRunner.cs` L7333 附近）；英雄卡 `hp` 字段恒 `0`（玩家血量存在 `Player.hp` 上）。
- **影响**：规则图若对英雄用它判"濒死"，结果永远为真 → 条件分支走错。
- **建议**：英雄特判 —— 若卡是英雄（`c.slot == Slot.Hero` 或 `c.uid == owner.hero.uid`），改读所属玩家 `Player.hp`。
- **状态**：挂账（待用户决定是否改语义）

## F2. `Card` **单值通道**只认部分集合节点
- **现象**：`101006 获取玩家的手牌` 走单值通道能取到单卡；`101007 墓地 / 101008 牌库 / 101009 延迟区 / 101011 道具 / 101013 暂存区` 走单值通道得到 `null`。
- **证据**：批次2 实测观测（`ResolveInputCard` 返回 null → 靠测试台通用兜底 `GetObjectInput` 才拿到 `List<Card>`）。
- **影响**：用户把"牌库/墓地/道具"等节点接到**单卡输入口**时静默拿到空值（不报错）。
- **建议**：`ResolveInputCard` 对集合类来源统一加"**取第一张**"兜底（与手牌行为对齐）。
- **状态**：挂账

## F3. 102029「获取卡牌所在牌堆」对英雄卡返回**空串**
- **现象**：`card←英雄卡` → Pile 通道返回空字符串（非 null）。
- **证据**：批次2 实测观测 `GetObjectInput 返回=—（类型 String）`。
- **影响**：后续用它做字符串判断/比较时结果不可预期。
- **建议**：确认英雄是否应返回"战场/英雄区"这类区域名；若英雄不属于任何堆，返回 null 而不是空串更安全。
- **状态**：挂账

## F4. `Pile`（牌堆）类型**没有可取源的节点** —— 牌堆族几乎无法连线
- **现象**：`105002 获取牌堆中的牌 / 105003 获取牌堆名 / 105004 获取牌堆所属玩家` 的 `pile` 输入口**找不到任何能产出 Pile 的节点**；`101017 获取牌堆` 返回的其实是**卡牌集合**（`List<Card>`），不是 Pile 对象。
- **证据**：批次3 实测 —— 105002 只能拿到空集合 `List(0)`；105003 返回空串；105004 竟返回了一个 Player（走的是"玩家口兜底"，**不是**来自牌堆）。
- **影响**：牌堆族 3 个节点在规则图里**接不出有意义的连线**（静默拿空值/兜底值）；105004 更会给出"看似成功"的错误答案。
- **建议**：① 让 `101017 获取牌堆` 输出真正的 Pile（或补一个"牌堆"取值节点）；② 105004 无 pile 输入时应返回 null，而不是回退到"施法方玩家"。
- **状态**：挂账

## F5. `GraphMap`（映射）走 Object 通道返回**空串**
- **现象**：`113001 创建映射集合 / 213001 设置映射 / 213002 移除映射` 的 `return:GraphMap` 经 Object 通道取值 → 得到 `""`（String 空串）。
- **证据**：批次3 实测观测 `GetObjectInput 返回=—（类型 String）`。
- **影响**：把映射接到 Object 口会**静默拿到空串**（不是 null、也不是映射本身）→ 后续比较/条件判断容易走偏。
- **建议**：Object 通道对 GraphMap 返回映射本身；无专用通道时也应返回 null 而不是空串。
- **状态**：挂账

## ⚠️ 已改代码、**待回归确认**（第二轮：F9 / F1）
| # | 改动 | 待验证点 |
|---|---|---|
| **F9** | `ResolveInputRecord`：记录口解析不出来源（或来源解析为空）时**回退"最近一条事件记录"** | 109002 非空 / 109003 = true / 109009 非空 |
| **F1** | `102018 是否濒死`：用 `owner.hero.uid` 判定英雄，英雄改看 `Player.hp` | 满血英雄 → **false**（修复前恒 true）|

> 回归脚本已就绪：`tools/batch_overrides.tsv` = 回归4（11 条：F1×1 + F9×3 + F6/F7/F10/F12 复测 + 邻域），
> `tools/node_batch.json` 也已按它生成 → **只要 Play 能跑起来，报告会自动产出**。
> 当前阻塞：Unity 的 Play 无法通过 MCP 启动（编辑器需人工点 ▶ 或重启 Unity）。

## ✅ 已修复（第一轮：F6 / F7 / F10 / F12）
| # | 修复内容 | 回归结果 |
|---|---|---|
| **F6** | `EvaluateDefineArray` 的 default 分支原为 `ResolveValueDefine(同一节点)`，而 103003 的 `ResolveValueDefine` 又回调本函数 → 无限互递归。改为解析**自身输入口**的定义（不再自我回溯）| ✅ 103003 不再崩（无引用来源时返回空）|
| **F7** | 103010/103011/103012 原对**自身**调 `ResolveValueDefine`（只认定义产出型节点）→ 恒 null。新增 `DefineInputOf()`（cardDefine → card → cardRef 依次解析）并替换调用点 | ✅ 103010 返回 **7**、103011 返回 **0**（终于取到定义）；断言已同步为"定义字段" |
| **F10** | 合成事件上下文时**没填 `turn`** → 108011 恒返回 0。在 `Run` 的合成分支补 `cur_event.turn = GameData.turn_count`（并保留 108011 内的兜底）| ✅ 108011 在 Run 内实测 **1**（= 回合数）|
| **F12** | `112007 获取临时变量` 只在 Boolean/Int 通道有分支，Object 通道缺 → 被当"未支持动作"跳过。补 Object 分支 | ✅ 不再出现"未支持动作"日志（值仍 null 是 Run 作用域所致，属正常）|

## F6. 103003「获取卡牌定义列表」在 `cardRefs` 用字段引用时**抛 StackOverflow** ✗
- **现象**：`cardRefs` 字段填一个卡牌 id（如 `lava_beast`）→ 取值时 `ResolveInputDefine` 抛 `The requested operation caused a stack overflow`（栈溢出，非普通异常！）。
- **证据**：批次4 实测（异常被测试台 try/catch 捕获，未中断对局）；实现为 `case "103003" → EvaluateDefineArray(...)`（`NodeDocRunner.cs:6262` 附近）。
- **影响**：栈溢出在 Unity 里极易直接崩掉进程（本路径下的递归没有 `probe_depth` 之类的保护）；任何用"定义列表"节点的图都有可能踩到。
- **建议**：给定义引用解析加**递归深度/环检测**（复用既有的 `probe_depth`）；`cardRefs` 直接按 id 查 `CardData.card_list` 即可，不必走节点回溯。
- **状态**：挂账（优先级高：崩溃级）

## F7. 103010/103011/103012（含同模式的 103013）取不到定义 → 恒返回 null ✗
- **现象**：`card ← 一个定义来源` 后，取"花费/攻击力/生命值"全部得到 `null`（走 Object 兜底变成空串）。
- **证据**：批次4 实测 3 例失败；实现为 `CardData d = ResolveValueDefine(logic, graph, src, ...)`（**对自身节点**求定义），而 `ResolveValueDefine` 只认"定义产出型"节点 → 自身不是 → null。**对照组**：103015 用 `ResolveInputDefine(logic, graph, node, "card", ...)`（解自己的 `card` 口）→ 实测**正常**。
- **建议**：把这几处（103010/103011/103012/103013 …）统一改为 `ResolveInputDefine(..., "card" 或 "cardDefine", ...)`（或先试 `cardDefine` 再试 `card`）。
- **补充（同族，实测也是空的）**：`103007 获取目标卡牌的定义列表`、`103015 获取英雄牌技能` 的观测值也是空串 —— 它们同样依赖 `EvaluateDefineArray` / `ResolveValueDefine` 的节点回溯，实测拿不到值；因为断言是"非 null"，空串被判成**假通过**（测试台已在本轮记录观测，不再当通过证据）。
- **状态**：挂账

## F8. 快照族（104001-104019 共 16 个）的 `card:CardSnapshot` 输入口**取不到快照来源** ✗
- **现象**：把快照产出节点（108008/108009/109007）接到快照族的 `card` 口后，族内节点拿不到快照：104011 返回 `null`，其余多数落到"当前卡克隆回退值"。
- **证据**：`SnapshotInput()` = `ResolveInputCard(act, "card")`（走 **Card 通道**）；而 108008/108009 只实现在 `ResolveSnapshot`（快照/Object 通道），109007 只在 Object 通道 → Card 通道没有这三个节点的分支。
- **影响**：快照族在规则图里**实际接不到真实快照**（拿到的是当前卡或 null），"某事件时的快照"语义失效。
- **建议**：让 Card 通道识别 108008/108009/109007 → 转调 `ResolveSnapshot` / 事件记录快照。
- **状态**：挂账（优先级高）

## F9. 109002/109003/109009（事件记录为输入的 3 个节点）取不到记录 ✗
- **现象**：把 `record` 口接到"最近一条事件记录"来源后 → 109002 返回 null、109003 返回 false、109009 返回 null。
- **对照**：同一批里 `109004/109005/109006`（记录**集合**输出）**正常**（断言非空通过）→ 说明事件记录本身是有的、`GraphEventLog` 正常。
- **推断**：问题出在"记录口（EventRecord）"的输入解析上（`ResolveInputRecord` 路径），而不是数据缺失。
- **建议**：让 `ResolveInputRecord` 优先用 Object 通道解析上游记录节点（与 111004/集合族的兜底一致），并加一条"来源是 109004 集合 → 取最后一条"的回归用例。
- **状态**：挂账

## F10. 108011「获取事件发生的回合」返回 0 ✗
- **现象**：在真实 Run 内用循环次数观测 → 伤害 0（即值=0）；同一张图里 `109008 获取当前回合数` 正确返回 **1**。
- **推断**：108011 读的是事件上下文里的回合字段，而 `GraphEventContext` 在运行期没有填这个字段（默认 0）。
- **建议**：`Run` 构造 `cur_event` 时把当前回合数写进事件上下文，或让 108011 回退到 `GameData.turn_count`。
- **状态**：挂账

## ⛔ 仍未修（第三轮：F4 / F8 / F11）—— 已定位到具体环节
> 测试台判据已修严（`value_notnull` **不再把空串算作有值**）→ 之前 F4/F8/F11 的"通过"是**空串假通过**，真实情况如下：

| # | 现状（严判据实测）| 下一步要查的点 |
|---|---|---|
| **F8** 快照族 | 104001 → `""` ✗、104002 → `""` ✗、104011 → `null` ✗（104007/104014 通过）| `SnapshotInput()`（= `ResolveInputCard(pin "card")`）链路上，快照来源节点 108008 是否真被当作 NodeDoc 取值节点（`src.category` 判定）；`ResolveValueCard` 里 108008 的分支是否与实测走的通道一致 |
| **F4** 牌堆族 | 105002 → `List(0)` ✗（空）、105003 → `""` ✗、105004 → 返回**兜底玩家** ✗ | `101017 获取牌堆` 产出的 `"玩家id\|区域"` 是否被 `ResolveInputPile`/`PileDecode` 正确解开（区域名规范 `PileNormalize` 是否匹配「牌库」）；另 105004 无有效 pile 时应返回 null |
| **F11** 增益族 | 106002 → `""` ✗（用 `buffs.json` 里的真实 id `buff_8c5cfda5`）| `BuffPoolIO.Get(id)` 期望的 id 形式（可能不是 json 里的 `"id"`，而是资源名/池键名）；106003 → false ✓ 说明定义口未命中 |

## F11. 增益族"没有可取源的增益定义" ✗（与 F4/F8 同类）
- **现象**：增益族 10 个节点里，凡是以 `buff` / `buffDefine` 为输入的（106004/106005/106007/206002/206003/206004）都只能记录观测（拿不到增益实例/定义）；`106002 获取增益定义` 是**唯一**的增益定义产出节点，但它的输入也是"引用字段"（buffDefine），没有引用来源。
- **对照**：`106003 是否具有增益`（card+定义口）与 `206001 添加增益` 至少能执行到"无定义"分支。
- **影响**：增益族在规则图里难以实际使用（增益定义来源缺失）。
- **建议**：补一个"按 id/按卡牌定义列举增益定义"的取值节点（类似 103002 获取卡牌定义的写法），或让 `106002` 支持从下拉选择。
- **状态**：挂账

## F12. 112007「获取临时变量」**实际未实现** ✗（覆盖清单的假阳性）
- **现象**：无论是否接线，取值都被跳过，日志明确输出：
  `[NodeDoc] 未支持的 NodeDoc 动作，已跳过: 112007 (获取临时变量)`。
- **证据**：批次6 实测（112007 走 Object 通道 → `null` + 上述日志）；同时 `212004 设置临时变量` 能正常执行 → 写入端在、读取端缺失。
- **根因线索**：112007 出现在**动作分派列表**里（`NodeDocRunner.cs:312` 附近），但取值通道（`GetObjectInput`/`ResolveNodeInt`…）没有它的分支 → 于是命中"未支持动作，已跳过"。
- **影响**：临时变量**写进去读不出来** → 该功能实际不可用。覆盖清单（判据 v3）把它算作"已实现"，属**假阳性**（case 命中 ≠ 取值通道实现）。
- **建议**：在 Object 通道补 `112007 → GetTempVar(node)`（已有现成 helper：`NodeDocRunner.cs:1272/1492` 就是同一 helper 的用法）。
- **状态**：挂账（修复优先级：高，改动小）

## 观察记录（非缺陷，供参考）
- `ResolveArrayCards` / `ResolveValueCards(...,"array")` 是"取该节点 `array` **输入口**"的通道，**不套用节点自身运算**（实测 111026 前 2 个 → 返回全部）；按上游节点语义求集合的是 `ResolveInputCards`。测试台已按此修正。
- 批次3 实测量值（手牌 5 张，攻击 [9,4,2,4,0]）：求和 **19**、最小 **0**、最大 **9**、平均 **3**、111031 属性映射 **[9,4,2,4,0]** ✓；111001 创建集合 **1** ✓、111002 添加元素 **5→6** ✓、111026 前2个 **2** ✓、111027 之外 **4** ✓、111028 随机2 **2** ✓、111029 条件恒真 **5** ✓、111030 剩余 **0** ✓、111011 相减(自身) **0** ✓、111018 相加 **10** ✓、遍历 211001 循环体伤害 **5** ✓。
