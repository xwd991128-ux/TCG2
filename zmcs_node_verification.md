# NodeDoc 节点接入验证清单（TCG2）

> 配套文档：`zmcs_node_progress.md`（进度与开发约定）。本文件只用于**填写测试结果**。
> 更新时间：2026-09-12　|　共 **68** 个待验证用例（覆盖本轮 T1/T2/T3 新增与改动节点）。
> 结果取值：`✅ 通过` / `❌ 失败` / `⏭ 跳过`。失败请填"实际现象"，并附 Console 中 `[NodeDoc] xxx` 日志或截图。

---

## 〇、填写说明

1. 每个用例只需填 3 行：**结果**、**实际现象**、**备注**。
2. 「预期」是判定基准；若实际与预期不符即为失败，请在备注写偏差。
3. 无法构造的场景可直接 `⏭ 跳过`，并在备注说明原因。
4. 全部填完后，把本文件的「结果」列汇总到下面「一、汇总表」。

---

## 一、汇总表

| 批次 | 用例数 | 通过 | 失败 | 跳过 | 负责人 | 备注 |
|---|---|---|---|---|---|---|
| T1 Pile 值通道 | 8 | | | | | |
| T2 事件日志 | 14 | | | | | |
| T2 卡牌快照 | 16 | | | | | |
| T2 增益 | 7 | | | | | |
| T3-a 玩家 | 3 | | | | | |
| T3-b 选择类 | 2 | | | | | |
| T3-c 动作/伤害/杂项 | 15 | | | | | |
| T3-d 查询 | 3 | | | | | |
| **合计** | **68** | | | | | |

---

## 二、通用前置条件（所有用例共用）

- [ ] Unity 编译通过，无报错。
- [ ] 打开图编辑器，节点库能看到新增节点（含 Pile 区域下拉、事件类型下拉、增益下拉）。
- [ ] Console 打开，能观察 `[NodeDoc] xxx` 日志。
- [ ] 准备测试卡：一张随从（可上场/可攻击）、若干张手牌/牌库/墓地卡、一张装备、一张奥秘。
- [ ] 保存图后重开，连线与字段保留正常。
- 备注（环境/版本/卡池）：

---

## 三、T1 Pile 值通道（8）

### V01 `102029 获取卡牌所在牌堆`
- 测试目标：卡牌 → 牌堆编码（`玩家id|区域名`）
- 步骤：分别取 战场/手牌/墓地/装备/奥秘 各一张卡 → 接 `102029` → 接 `105003` 与 `105004`
- 预期：名称依次为 战场/手牌/墓地/装备/奥秘；玩家 id 与卡归属一致
- 边界：卡在暂存区或不在任何区 → 输出空，下游无值
- 异常：`card` 口未连线 → 无输出且不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V02 `105002 获取牌堆中的牌`
- 测试目标：牌堆 → 卡牌集合
- 步骤：`102029(手牌某卡)` → `105002` → `111004 获取元素数量`
- 预期：数量 = 该区域实际卡数
- 边界：空区域 → 0；Pile 口未连线 → 用字段默认区（施法者牌库）
- 异常：手动填非法编码 → 返回空集合，不报错
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V03 `105003 获取牌堆名`
- 测试目标：牌堆 → 字符串
- 步骤：`102029` → `105003`
- 预期：返回 手牌/牌库/墓地/战场/装备/奥秘
- 边界：空/非法 Pile → 无值
- 异常：不产生异常日志
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V04 `105004 获取牌堆所属玩家`
- 测试目标：牌堆 → 玩家
- 步骤：取对手区域的卡 `102029` → `105004` → 读玩家属性
- 预期：返回该区所属玩家
- 边界：玩家 id 非法 → null
- 异常：玩家口未连 → 不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V05 `200004 创建牌堆`
- 测试目标：不实际创建，输出固定区域句柄
- 步骤：`200004(pileName=牌库)` → `105002` → `111004`
- 预期：数量 = 该玩家牌库张数；日志含 "200004 创建牌堆（TCG2 近似=固定区域）"
- 边界：`pileName` 填自定义名 → 下游为空
- 异常：玩家口未连 → 默认施法者
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V06 `200005 删除牌堆`
- 测试目标：清空指定区域
- 步骤：让墓地有 N 张 → `200005(pile=墓地)` → `105002` 再数
- 预期：结果 0；日志"删除牌堆 pid|墓地 清空 N 张"
- 边界：空区域 → 0 张
- 异常：非法 Pile → 警告"未解析/非法区域"；对**战场**执行 → 卡走离场流程进墓地、无残留
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V07 `200006 从牌堆将卡牌移动到目标牌堆`
- 测试目标：跨区移动
- 步骤：手牌卡 → `200006(toPile=牌库, position=-1)`；再 `牌库→手牌, position=0`
- 预期：卡出现在目标区末尾 / 首位；日志"200006 移动卡牌 xx → pid|牌库 成功"
- 边界：`position` 超范围 → 夹到末尾
- 异常：目标=战场/装备 → 失败日志（拒绝）；场上卡或跨玩家 → 失败
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V08 `202046 创建衍生卡并置入牌堆`
- 测试目标：创建并落位
- 步骤：`cardDefine` 选一张 → `202046(pile=手牌/牌库/墓地/奥秘/战场/装备, position)`
- 预期：对应区新增 1 张；战场自动空位并触发入场
- 边界：未连 `pile` → 落暂存区 `cards_temp`（日志明示）
- 异常：战场无空位 → 失败警告；`cardDefine` 空 → 警告"卡牌定义不存在"
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

---

## 四、T2 事件日志（14）

**前置**：用图事件入口「伤害时 / 伤害后」或「主动效果」建图，数据口从 `108001 当前事件` 引出。
**重要限制**：`208009` 只记录重复次数供 `108010` 读，引擎不会据此重放事件。

### V09 `108004 事件类型判断`
- 测试目标：事件类型匹配
- 步骤：伤害事件图内 事件+类型=伤害 → 接分支动作；改类型=死亡
- 预期：前者为真，后者为假
- 边界：事件口未连线 → 默认当前事件；无广播上下文 → 假
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V10 `108005 获取本局游戏事件`
- 测试目标：按类型取全集
- 步骤：触发 3 次伤害事件后取"伤害"集合 → 数元素
- 预期：含本次及历史各次
- 边界：无匹配 → 空集合；日志上限 512，超限丢最旧
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V11 `108006 获取本回合事件`
- 测试目标：本回合全集
- 步骤：第 2 回合取一次并计数
- 预期：只含本回合事件
- 边界：跨回合后旧回合事件不计入
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V12 `108007 获取前X-Y回合内的事件`
- 测试目标：回合区间筛选
- 步骤：farther=1, nearer=0
- 预期：覆盖上一回合起至今
- 边界：X<Y → 按 max/min 容错；X=Y=0 → 仅本回合
- 异常：负值 → 不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V13 `108008 获取卡牌在某事件前的快照`
- 测试目标：事件前状态
- 步骤：OnBeforeDamage 图内 `108008(卡,当前事件)` → `104009 当前生命值`
- 预期：HP 高于伤害后
- 边界：请求卡≠事件主体卡 → 回退返回当前卡克隆
- 异常：无事件口 → 回退，不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V14 `108009 获取卡牌在某事件后的快照`
- 测试目标：事件后状态
- 步骤：OnAfterDamage 图内同上
- 预期：HP 低于事件前
- 边界：非主体卡 → 回退克隆
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V15 `108010 获取事件重复次数`
- 测试目标：读 repeat
- 步骤：①默认事件直接读；②`208009` 设 3 后读
- 预期：①1　②3
- 边界：无事件上下文 → 返回 1
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V16 `108011 获取事件发生的回合`
- 测试目标：读 turn
- 步骤：第 2 回合触发并读取
- 预期：2
- 边界：开局事件（回合 0）→ 0
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V17 `108012 获取事件的父事件`
- 测试目标：父子链
- 步骤：A 事件链内触发 B → 对 B 取父事件
- 预期：返回 A
- 边界：顶层事件 → 无值
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V18 `108013 获取事件的子事件`
- 测试目标：直接子事件
- 步骤：对 A 取子事件集合
- 预期：含 B，不含 B 的孙事件
- 边界：无子 → 空集合
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V19 `108014 获取事件发生的事件链`
- 测试目标：父→祖父链
- 步骤：三层嵌套后对最内层取链
- 预期：顺序=[父, 祖父]，不含自身
- 边界：顶层 → 空集合
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V20 `108015 获取事件发生时引发的事件`
- 测试目标：Before 阶段子事件
- 步骤：对 A 取 Before 子事件
- 预期：只含 Before 阶段
- 边界：无子 → 空
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V21 `108016 获取事件发生后引发的事件`
- 测试目标：After 阶段子事件
- 步骤：对 A 取 After 子事件
- 预期：只含 After 阶段
- 边界：无子 → 空
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V22 `208009 设置事件重复次数`
- 测试目标：写 repeat
- 步骤：事件口 + times=3 → `108010` 读
- 预期：读到 3；不产生额外执行
- 边界：无事件上下文 → 警告"无事件上下文，忽略"
- 异常：times 为负 → 原样写入（请确认是否需要夹取）
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

---

## 五、T2 卡牌快照家族（16）

**前置**：`108008`（前）或 `108009`（后）输出接各 `104xxx`。

### V23 `104001 获取卡牌快照对应卡牌`
- 测试目标：快照 → 卡
- 步骤：快照 → `104001`；随后改动原卡属性，再读快照卡
- 预期：返回快照记录的卡，且**不随原卡变化**（深克隆）
- 边界：空快照 → 无值
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V24 `104002 获取卡牌快照卡牌定义`
- 测试目标：快照 → 定义
- 预期：返回当时 CardData
- 边界：空快照 → 无值
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V25 `104004 卡牌快照关键词判断`
- 测试目标：关键词判断
- 步骤：快照 + 关键词下拉（选一个该卡有的/没有的）
- 预期：有=真，无=假
- 边界：关键词为空 → 假
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V26 `104005 获取卡牌快照属性`
- 测试目标：属性读取
- 步骤：快照 + 属性名（攻击/生命/法力费用/未知名）
- 预期：已知名返回对应值；未知名按攻击
- 边界：空快照 → 无值
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V27 `104006 获取卡牌快照花费`
- 测试目标：花费读取
- 预期：快照时刻的法力费用
- 边界：空快照 → 无值
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V28 `104007 获取卡牌快照攻击力`
- 测试目标：攻击力读取
- 预期：快照时刻攻击力（含当时加成）
- 边界：空快照 → 无值
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V29 `104008 获取卡牌快照最大生命值`
- 测试目标：最大生命读取
- 预期：快照时刻 `GetHPMax`
- 边界：空快照 → 无值
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V30 `104009 获取卡牌快照当前生命值`
- 测试目标：当前生命读取
- 预期：快照时刻 `GetHP`
- 边界：空快照 → 无值
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V31 `104010 获取卡牌快照拥有者`
- 测试目标：快照 → 玩家
- 预期：当时归属玩家
- 边界：玩家不存在 → null
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V32 `104011 获取卡牌快照护甲值`
- 测试目标：护甲读取
- 预期：快照时刻 Armor 状态值
- 边界：无护甲 → 0
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V33 `104014 卡牌快照具有宣言`
- 测试目标：≈定义带 OnPlay 能力
- 预期：有 OnPlay=真
- 边界：无定义 → 假
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V34 `104015 卡牌快照具有遗言`
- 测试目标：≈定义带 OnDeath 能力
- 预期：有 OnDeath=真
- 边界：无定义 → 假
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V35 `104016 卡牌快照是否是陷阱`
- 测试目标：陷阱（Secret）判断
- 预期：Secret=真
- 边界：快照口名为 `cardSnapshot`，未连 → 假
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V36 `104017 卡牌快照类型判断`
- 测试目标：类型判断
- 步骤：快照 + 卡牌类型下拉
- 预期：匹配=真
- 边界：类型下拉空 → 假
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V37 `104018 卡牌快照拥有标签`
- 测试目标：标签判断
- 步骤：快照 + 标签文本（关键词/特性 id 或标题）
- 预期：匹配=真
- 边界：标签为空 → 假
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V38 `104019 获取卡牌快照标签列表`
- 测试目标：标签集合
- 预期：返回关键词+特性 id 列表
- 边界：无标签 → 空列表
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

---

## 六、T2 增益家族（7）

**前置**：用 `206001 添加增益` 给卡加一个含自定义属性的增益；或建「增益触发」图。

### V39 `106001 该增益`
- 测试目标：取当前增益
- 步骤：在增益触发图内直接输出该节点
- 预期：返回当前增益
- 边界：普通图（非增益图）→ 无值
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V40 `106002 获取增益定义`
- 测试目标：选固定增益定义
- 步骤：下拉选一个增益 → 接 `106005` 或后续
- 预期：返回所选 BuffData
- 边界：未选 → 无值
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V41 `106003 是否具有增益`
- 测试目标：卡 + 增益 id 判断
- 预期：有=真
- 边界：卡空 / buff_id 未填 → 假
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V42 `106005 获取单个增益的定义`
- 测试目标：增益 → 定义
- 步骤：`106001`/`106006` → `106005`
- 预期：返回对应 BuffData
- 边界：未连 Buff 口 → 默认当前增益；两者都无 → 无值
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V43 `106006 获取卡牌上的所有增益`
- 测试目标：卡 → 增益集合
- 预期：数量 = 卡上增益数
- 边界：无增益 → 空集合
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V44 `206004 设置增益属性可见性`
- 测试目标：写可见性
- 步骤：`206004(增益, 属性名, 玩家, visible)`
- 预期：写入成功，日志显示作用增益个数
- 边界：属性名空 / 玩家口空 → 不写（计数 0）
- 异常：不崩溃；只存不发（不影响界面显示）
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V45 `106007 增益属性是否可见`
- 测试目标：读可见性（与 V44 组成往返）
- 步骤：先 `206004 visible=true` → `106007`；再 `visible=false` → `106007`
- 预期：真 → 假
- 边界：未设置过 → 假
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

---

## 七、T3-a 玩家（3）

### V46 `101020 获取所有玩家`
- 测试目标：玩家集合
- 步骤：接 `111004` 数元素
- 预期：含双方玩家
- 边界：单消费者（如玩家口）只取首个（v1 限制）
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V47 `101022 获取某个回合的玩家`
- 测试目标：回合 → 玩家（近似）
- 预期：返回当前回合玩家
- 边界：传其它回合号 → 仍返回当前回合玩家（已知近似）
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V48 `201002 设置玩家属性`
- 测试目标：写玩家属性
- 步骤：分别设置 生命/最大生命/灵力/灵力上限/击杀数 → 用 `101002` 读回
- 预期：读回值 = 设置值
- 边界：设置负数 → 生命/上限夹到 0；玩家口未连 → 默认施法者
- 异常：无玩家 → 警告并跳过
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

---

## 八、T3-b 选择类（2）

### V49 `201004 从一定数量的卡牌中选择一张`
- 测试目标：复用入口「目标1条件」选目标
- 步骤：入口目标类型=角色，候选集合含多张 → 打出时选其中一张
- 预期：节点输出 = 玩家所选那张
- 边界：入口未选 / 目标不在候选内 → 回退首张
- 异常：候选空 → 无值；AI/自动结算 → 回退首张
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V50 `201005 从一定数量的卡牌定义中选择一张`
- 测试目标：同上（定义）
- 预期：= 所选目标卡的定义
- 边界：回退取首个定义
- 异常：候选空 → 无值
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

---

## 九、T3-c 动作 / 多目标伤害 / 杂项（15）

### V51 `202011 造成法术伤害`
- 测试目标：集合目标法伤
- 预期：每张目标受 `damage` 点法伤（无视护甲）
- 边界：目标空 → 若入口选了玩家则打玩家
- 异常：目标与玩家都空 → 仅日志，不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V52 `202012 造成固定法术伤害并分配给随机目标`
- 测试目标：逐点随机分配
- 预期：共 `damage` 次、每次随机 1 点法伤
- 边界：`damage=0` 或目标空 → 无效果
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V53 `202017 强制更新游戏状态`
- 测试目标：刷新数据
- 预期：日志"强制更新游戏状态"，持续效果/界面数值刷新
- 边界：无参数
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V54 `202021 触发卡牌宣言`
- 测试目标：≈触发 OnPlay 能力
- 步骤：对一张有 OnPlay 的卡执行
- 预期：OnPlay 效果被触发
- 边界：卡无 OnPlay → 无效果但不异常
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V55 `202022 触发卡牌遗言`
- 测试目标：≈触发 OnDeath 能力
- 预期：OnDeath 效果被触发
- 边界：卡无 OnDeath → 无效果
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V56 `202023 触发法术或技能卡牌效果`
- 测试目标：近似=按 OnPlay 触发
- 预期：与 V54 行为一致
- 边界：同上
- 异常：请确认"近似 OnPlay"是否可接受
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V57 `202024 重置卡牌`
- 测试目标：复位卡牌
- 步骤：给卡加加成/状态后执行
- 预期：加成/状态/增益清空、基础值复位
- 边界：空集合 → 无
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V58 `202025 强制攻击目标`
- 测试目标：强制攻击
- 预期：逐张对首目标卡 / 选中玩家发起正常战斗结算
- 边界：目标为空且无玩家 → 无动作
- 异常：忽略费用/条件（设计如此）
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V59 `202026 本回合攻击次数增加一次`
- 测试目标：允许再攻击
- 步骤：让一张已攻击的随从执行
- 预期：该卡可再次攻击
- 边界：未攻击过的卡 → 无变化
- 异常：多次叠加是否有上限（请确认）
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V60 `202027 揭示卡牌`
- 测试目标：奥秘区卡 → 墓地
- 预期：卡进墓地并走正常结算
- 边界：不在奥秘区 → 仅日志，不改变状态
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V61 `210008 卡牌移动到暂存区`
- 测试目标：移入 cards_temp
- 预期：卡从原区移入暂存区
- 边界：玩家口未连 → 默认施法者
- 异常：跨玩家搬运直接移入目标玩家暂存区（请确认是否符合预期）
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V62 `202035 造成伤害或法伤并分配给目标`
- 测试目标：总伤害均分
- 预期：总伤害在目标间均分，余数给前排
- 边界：目标空或总伤害 0 → 无
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V63 `202042 造成伤害或法伤并分配给目标（变体）`
- 测试目标：同 V62（参数顺序不同）
- 预期：同 V62
- 边界：同 V62
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V64 `202036 对固定数量的随机目标造成伤害或法伤`
- 测试目标：随机 N 目标
- 预期：随机命中 `count` 次、每次 `damage`
- 边界：目标空 → 无；`count` 大 → 允许重复命中同一目标
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V65 `202043 对固定数量的随机目标造成伤害或法伤（变体）`
- 测试目标：同 V64
- 预期：同 V64
- 边界：同 V64
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

---

## 十、T3-d 查询（3）

### V66 `102009 获取唯一属性名`
- 测试目标：字符串透传
- 预期：原样输出 `propName` 字段
- 边界：空 → 空串
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V67 `102035 获取卡牌属性名`
- 测试目标：属性取值器 → 中文名
- 预期：返回 攻击/生命/法力费用
- 边界：未选 → 默认"攻击"
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

### V68 `103003 获取卡牌定义列表`
- 测试目标：定义集合
- 步骤：接 `111008 随机元素` 或 `111004` 数元素
- 预期：近似=全部卡牌定义
- 边界：单定义消费者取首个
- 异常：不崩溃
- 结果：☐ ✅ 通过　☐ ❌ 失败　☐ ⏭ 跳过
- 实际现象：
- 备注：

---

## 十一、不可测 / 不作验证的节点（登记备查）

| 节点 | 原因 |
|---|---|
| `104012 卡牌快照是否已蓄力` | 蓄力载体未定义，已从白名单撤下 |
| `104013 获取卡牌快照蓄力层数` | 同上 |
| `201016 从卡牌中选择一张（带玩家口版）` | 与 201004 同名，库内隐藏；逻辑同 V49 |
| `201017 从卡牌定义中选择一张（带玩家口版）` | 与 201005 同名，库内隐藏；逻辑同 V50 |
| `202007 设置属性` | 摘要「已过时」，按约定不实现 |
| `109001~109009 事件记录家族` | 摘要「已过时」，已被 108 事件家族取代 |
| `208003~208008` | 摘要「已过时」 |
| `107002 / 107008 / 107010` | 无对应枚举，明确跳过 |

---

## 十二、全局收尾检查（必填）

- [ ] Unity 编译无报错、无新增警告。
- [ ] 节点库新端口/新下拉显示正确。
- [ ] 图保存 → 重开 → 连线与字段无丢失。
- [ ] 完整打出一张卡冒烟通过，无异常抛栈。
- [ ] AI 回合无异常（事件在 `is_ai_predict` 下不落日志）。
- 全局备注：

---

## 十三、问题记录区（自由填写）

| 序号 | 节点/用例 | 现象 | 复现步骤 | 期望 | 优先级 |
|---|---|---|---|---|---|
| 1 | | | | | |
| 2 | | | | | |
| 3 | | | | | |
| 4 | | | | | |
| 5 | | | | | |
