# 更新记录 / Changelog

## 2026-09-10 规则编辑器大版本：整页 TMP 统一 + 多效果图 + 事件/取值节点批量落地

### 新功能

**卡牌多效果图（对齐醉梦传说）**
- `CardCustomData.effects`：一张卡可有多张效果图（战吼/亡语/光环/事件…），**每张图一个入口**，彻底消除「一条图里多个入口谁生效」的歧义
- 旧单图 `graph` 字段双写兼容（`EnsureEffects()` 自动迁移），旧数据/旧工具零改动
- `CardPoolIO` 逐效果图编译为互相独立的能力；编辑器顶部效果图 Tab（自动按入口命名：主动效果/被动效果/光环效果/事件效果，支持新建/切换/删除）

**事件家族节点（对齐 zmcs）**
- `108001 当前事件` / `108002 获取变量` / `108003 转换事件类型` / `208001 设置变量` / `208002 阻止事件`
- 「当前事件」按执行链隔离（能力触发链压栈），一张卡多条链互不串扰

**取值节点接入（7 个）**
- `102003 卡牌关键词判断` / `102004 获取属性` / `103006 卡牌定义关键词判断` / `103014 是否为衍生牌` / `103020 获取卡牌定义属性` / `103021 卡牌定义是否是陷阱` / `103023 获取卡牌定义的类型`
- 端口严格按 `NodeDoc.xml` 对齐；关键词匹配支持 id 与标题

**端口行内常量控件（通用机制）**
- 字段与输入端口同名时：**无连线 → 端口行右侧直接显示可编辑控件**（输入框/下拉/多选/开关）；**有连线 → 控件隐藏、值走连线**，断开自动恢复
- 节点高度/宽度估算同步适配，不多占行、不越界

**关键词 / 种族可配置**
- 关键词：新增属性行 + **多选下拉**（列出游戏自带关键词，显示标题、存 `KeywordData.id`，可自定义输入）
- 种族：单选下拉 → **多选 + 自定义**；`CardCustomData.traits` 列表（`EnsureTraits()` 兼容旧单字段），`CardPoolIO.ApplyTraits()` 双向兼容

**卡牌属性区 UI**
- 音效 4 行新增「▶」**点击试听**（走 `CardAudioLoader` 解码缓存）；**导入限制 ≤10 秒**（超长拒绝并提示实际时长）
- 短字段两两一行：`类型|阵营`、`稀有度|费用`、`攻击|生命`
- 类型/阵营/稀有度：旧 UGUI Dropdown → **TMP 选择按钮 + 弹层**（显示名↔实际值映射，单选/多选/自定义统一走一套弹层）

**右侧栏 Tab**
- 新增 `卡牌参数 | 节点库` Tab，两面板从上下各半改为**各占满一整列**（显示内容翻倍），点选高亮切换

**整页 TMP 统一**
- 面板字段全面迁移：标题/状态/数量/提示 → `TMP_Text`；12 个输入框 → `TMP_InputField`
- 生成工具新增 `TmpifyPage()`：生成时把残留旧版 `Text`/`InputField` 一次性迁为 TMP（含 Toggle 勾选标记重绑）
- 运行时兜底迁移 `EnsureTmpUI()`：场景未重跑生成工具也能用（按行标签/名字重新定位绑定）
- **字号统一**：区块标题 20 / 状态条 18 / 行标签与按钮 15 / 正文 16

**图事件系统修复**
- 死亡/弃牌「时/后」事件把事件主体卡作为额外宿主（`extra_host`）：卡进墓地后**自身的亡语图也能触发**（不再受生效牌堆过滤拦截）

### Bug 修复
- `MainMenu.Update()`：场景缺 `MatchmakingPanel` 时 NRE（`loader` 一并判空）
- `CardPoolIO.CompileOneGraphAbilities`：拆方法后残留 `return result`（CS0127）
- 节点库重复的旧「死亡时」入口（OnDeath）删除，与新事件入口去重

---

## Bug 教训（本次开发沉淀）

1. **事件主体卡自身区域会变**：死亡/弃牌事件发出时卡已进墓地，若入口只按生效牌堆过滤宿主，卡自己的图永远收不到 → **事件主体一律作为 extra_host，不受 zone 限制**。
2. **外部场景引用必须判空**：场景里缺 `MatchmakingPanel` 就 NRE。UI 引用一律 `!= null` 再用（尤其 `Update()` 每帧跑的）。
3. **拆方法要检查返回语句**：`List<T>` 方法改成 `void` 后尾部残留 `return result;` → CS0127。
4. **TMP 输入框必须设置 `textViewport`**：缺了光标无法定位，表现为「无法输入」。同时文本/占位符 `raycastTarget = false`，否则文本挡住点击。
5. **未激活的层级上 `AddComponent` 会失败返回 null**（典型：`Open()` 先于 `Show()` 被调用）：
   - 依赖 AddComponent 的 UI 构建/迁移**推迟到激活后的第一帧**（`Update()` 里查标记执行）；
   - `StartCoroutine` 在失活物体上不会跑，不能用它做这种延迟；
   - `AddComponent` 结果必须判空 + try/catch，失败告警跳过，绝不 NRE。
6. **面板字段改 TMP 类型后，旧场景的序列化引用会变 None**：运行时迁移不能依赖字段引用，要**按结构定位**（属性行标签 → 行内控件；`Find` 名字），且"找不到时保留现值"，不要把有效引用清成 null。
7. **组件替换迁移的顺序**：先创建并配置新组件 → 全部成功后才销毁旧组件；失败要销毁半成品、保留旧组件，避免叠影和崩溃。每个物体只迁移一次（HashSet 去重），防止重复 AddComponent。
8. **面板字段类型改动会牵连生成工具编译**：`InputField→TMP_InputField` 这类改动必须同步改生成工具的创建方法（`CreateInputIn`/`CreateAudioRow` 等），否则编译不过。
9. **字段画到端口行内时，布局估算要同步**：高度估算要排除"已落到端口行"的字段（否则多空行），宽度要按"圆点+端口名+控件"计（否则控件越界）。
10. **多选/单选弹层做一层「显示名 ↔ 实际值」映射**（标题显示、id 存储）：后续换数据源（如自定义关键词池）时 UI 与语义都不用改。
